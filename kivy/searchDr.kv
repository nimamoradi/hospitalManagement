# Reference searchDr.py
#: import main searchDr
#: import ListAdapter kivy.adapters.listadapter.ListAdapter
#: import ListItemButton kivy.uix.listview.ListItemButton
#:import label kivy.uix.label
#:import sla kivy.adapters.simplelistadapter

SearchDr:
 
<SearchDr>:
    orientation: "vertical"
    search_list: search_list_view
    padding: 10
    spacing: 10
    BoxLayout:
        orientation: "vertical"
        size_hint_y: None
        height: "40dp"
        padding: [10,700,10,10]
        spacing: 20
        
    ListView:
        id: search_list_view
        adapter:
            # ListAdapter(data=["test"],cls=label.Label)
            sla.SimpleListAdapter(data=["Item #{0}".format(i) for i in range(100)], cls=label.Label)
